# Display Project

Simple Streamlit app with:
- a landing page to collect `database path`, `fps`, and `episode`
- a data display page that shows selected values

## Requirements

- Python 3.10+ (or compatible local Python 3)
streamlit run main.py


## Run

